<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit comment</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1>Edit Comment</h1>

<form action="/posts/<%= currentPost._id %>/comments/<%= currentComment._id %>?_method=PUT" method="POST">
  
  <input type="hidden" name="commenter" value="<%= user._id %>">
      
  <label for="text">Comment: </label>
  <textarea name="text" id="text" placeholder="Leave a comment ....." required><%= currentComment.text %></textarea>

  <input type="hidden" name="createdAt" id="createdAt" value="<%= dateTime %>">

  <button type="submit">Update comment</button>

</form>

<a href="/posts/<%= currentPost._id %>">Cancel</a>

</body>
</html>